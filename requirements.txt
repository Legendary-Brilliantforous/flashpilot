# Native GUI: PyQt6
PyQt6>=6.5

# MDM provisioning QR generation (used by the MDM/QR flow)
segno>=1.6

# LZ4 firmware decompression
lz4>=4.0

# Samsung FUS firmware downloader & decryption
samloader>=0.2.0
requests>=2.28.0
